services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_MULTIPLE_DATABASES: FreelanceUserProfiles,FreelanceIdentity
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - freelance-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - freelance-network
    healthcheck:
      test: timeout 10 bash -c ':> /dev/tcp/127.0.0.1/5672'
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 40s

volumes:
  postgres_data:
  rabbitmq_data:

networks:
  freelance-network:
    driver: bridge